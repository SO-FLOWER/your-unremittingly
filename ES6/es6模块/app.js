import { uniq } from 'loadsh';
import name from './src/config';

const arr = [1, 1, 12, 2, 4, 56, 7]
const user = new name('jsons', 'he is a compuctor use');
console.log(user);
console.log(uniq(arr));